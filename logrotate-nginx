/var/log/nginx/*.log {
  su root root
  daily
  missingok
  rotate 14
  compress
  delaycompress
  notifempty
  create 0640 root root
  sharedscripts
  postrotate
    which nginx >/dev/null 2>&1 && nginx -s reopen || true
  endscript
}
